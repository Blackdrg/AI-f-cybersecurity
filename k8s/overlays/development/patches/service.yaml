---
apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: NodePort  # Expose externally for dev
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
    - name: grpc
      port: 50051
      targetPort: 50051
      nodePort: 30051
