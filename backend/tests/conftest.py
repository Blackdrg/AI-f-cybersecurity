[[{'bbox': [100, 100, 200, 200], 'landmarks': [[125, 125], [175, 125], [150, 175]], 'confidence': 0.99}], {'person_id': 'pers_abc123', 'name': 'John Doe', 'age': 30, 'gender': 'male', 'embeddings': ['emb_001', 'emb_002'], 'consent_record': {'consent_record_id': 'cons_abc', 'granted_at': '2026-04-28T12:00:00Z', 'consent_text_version': 'v1'}}, {'top_k': 1, 'threshold': 0.6, 'enable_spoof_check': True}, {'name': 'Jane Doe', 'consent': True, 'age': 25, 'gender': 'female'}, {'user_id': 'usr_test123', 'role': 'operator', 'org_id': 'org_test456', 'subscription_tier': 'pro', 'exp': 'datetime.utcnow() + timedelta(hours=1)).timestamp()'}, {'Authorization': 'f'}, '---------------------------', '---------------------------', 'sample_image_bytes()', 'Load a sample test image."', '\n    # Create a synthetic 224x224 RGB image with a simple face-like ellipse\n    img = np.zeros((224, 224, 3), dtype=np.uint8)\n    # Draw a face-like ellipse\n    cv2.ellipse(img, (112, 112), (80, 100), 0, 0, 360, (200, 150, 100), -1)\n    # Eyes\n    cv2.circle(img, (90, 90), 10, (255, 255, 255), -1)\n    cv2.circle(img, (134, 90), 10, (255, 255, 255), -1)\n    # Nose\n    cv2.line(img, (112, 100), (102, 120), (150, 100, 80), 3)\n    # Mouth\n    cv2.ellipse(img, (112, 150), (25, 10), 0, 0, 180, (150, 80, 80), -1)\n    \n    # Encode to JPEG bytes\n    _, encoded = cv2.imencode(\'.jpg\', img)\n    return encoded.tobytes()\n\n@pytest.fixture\ndef sample_video_frames(sample_image_bytes):\n    "', 'Generate a sequence of frames from sample image."', '\n    import cv2\n    nparr = np.frombuffer(sample_image_bytes, np.uint8)\n    img = cv2.imdecode(nparr, cv2.IMREAD_COLOR)\n    \n    # Create 10 frames with slight variation\n    frames = []\n    for i in range(10):\n        # Add noise\n        noisy = img + np.random.randint(-5, 5, img.shape, dtype=np.int16)\n        noisy = np.clip(noisy, 0, 255).astype(np.uint8)\n        _, encoded = cv2.imencode(\'.jpg\', noisy)\n        frames.append(encoded.tobytes())\n    \n    return frames\n\n# ----------------------------\n# Metrics fixtures\n# ----------------------------\n\n@pytest.fixture\ndef mock_metrics(monkeypatch):\n    "', 'Mock Prometheus metrics to avoid conflicts in tests."', '\n    from app import metrics\n    \n    # Replace real counters with mocks\n    metrics.recognition_count = MagicMock()\n    metrics.recognition_count.inc = MagicMock()\n    metrics.enroll_count = MagicMock()\n    metrics.enroll_count.inc = MagicMock()\n    \n    return metrics\n\n# ----------------------------\n# Cleanup helpers\n# ----------------------------\n\n@pytest.fixture(scope="session', 'event_loop()', 'Create event loop for async tests."', '\n    loop = asyncio.get_event_loop_policy().new_event_loop()\n    yield loop\n    loop.close()']