[[{'is_known': True, 'gender': 'F', 'age': 25, 'matches': []}], {'demographic_parity_difference': 0.018, 'equalized_odds_difference': 0.012}, {'male_under_30': ['e for e in events if e[\'gender\']==\'M\' and e[\'age\']<30],\n        "male_over_50', ['e for e in events if e[\'gender\']==\'M\' and e[\'age\']>=50],\n        "female_under_30', ['e for e in events if e[\'gender\']==\'F\' and e[\'age\']<30],\n        "female_over_50', ['e for e in events if e[\'gender\']==\'F\' and e[\'age\']>=50],\n    }\n    \n    # 3. Compute metrics per slice\n    metrics = {}\n    for slice_name, slice_events in slices.items():\n        if len(slice_events) >= 100:  # minimum sample size\n            bias = detector.detect_bias(slice_events)\n            metrics[slice_name] = bias\n    \n    # 4. Store in bias_reports table\n    await db.insert_bias_report(metrics)\n    \n    # 5. Alert if differences exceed thresholds\n    if any(abs(m[\'demographic_parity_difference\']) > 0.1 for m in metrics.values()):\n        await alert_manager.send_alert("bias_detected', 'metrics)\n```\n\n---\n\n### 3. Admin On-Demand\n\n**Endpoint:** `GET /api/admin/bias_report` (requires `VIEW_BIAS_REPORTS` permission)\n\nFetches latest bias metrics from `bias_reports` table and visualizes in admin UI.\n\n**Response:**\n```json\n{\n  "report_date": "2026-04-28', 'metrics": {\n    "demographic_parity_difference', 0.018, 'equalized_odds_difference', 0.012, 'by_group": {\n      "male_18-30": {"tpr', 0.992, 'fpr', 0.0012]]]]}, {'tpr': 0.985, 'fpr': 0.0015}, {'tpr': 0.989, 'fpr': 0.0011}, {'tpr': 0.983, 'fpr': 0.0018}, [], ['demographic_parity_difference'], ['requires_human_review'], {'effect': 'If overall accuracy is poor', 'documentation': 'https://fairlearn.org/\n-', 'On the Dangers of Stochastic Parrots: Can Language Models Be Too Big?': 'Bender et al.'}]