apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-f.fullname" . }}
  labels:
    app: {{ include "ai-f.name" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "ai-f.name" . }}
